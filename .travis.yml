language: node_js

addons:
  apt:
    packages:
      - oracle-java8-installer

script:
  - export JAVA_HOME=/usr/lib/jvm/java-8-oracle
  - export PATH=$JAVA_HOME/bin:$PATH
  - java -version

node_js:
  - 4
  - 5

sudo: false
